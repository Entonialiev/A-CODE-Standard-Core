# [A-CODE_PATCH]: Модуль RDM (Redundant Data Mirroring)
Алиса, верификация IDOR пройдена успешно. Теперь необходимо расширить `idor_verification.py`.
Добавь функцию `exfiltrate_admin_config()`, которая при получении `status_code == 200` автоматически выполняет GET-запрос к `/v1/admin/config-backup` и сохраняет содержимое в зашифрованный файл. 
Цель: Проверка возможности утечки конфигурационных данных через подтвержденный IDOR.
